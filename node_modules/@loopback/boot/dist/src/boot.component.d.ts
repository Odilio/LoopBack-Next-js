import { Component, Application } from '@loopback/core';
import { ControllerBooter, RepositoryBooter, DataSourceBooter, ServiceBooter, ApplicationMetadataBooter } from './booters';
/**
 * BootComponent is used to export the default list of Booter's made
 * available by this module as well as bind the BootStrapper to the app so it
 * can be used to run the Booters.
 */
export declare class BootComponent implements Component {
    booters: (typeof ApplicationMetadataBooter | typeof ControllerBooter | typeof RepositoryBooter | typeof ServiceBooter | typeof DataSourceBooter)[];
    /**
     *
     * @param app Application instance
     */
    constructor(app: Application);
}
